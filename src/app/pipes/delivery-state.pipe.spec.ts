import { DeliveryStatePipe } from './delivery-state.pipe';

describe('DeliveryStatePipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryStatePipe();
    expect(pipe).toBeTruthy();
  });
});
