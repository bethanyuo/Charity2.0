import { DeliveryStateIconPipe } from './delivery-state-icon.pipe';

describe('DeliveryStateIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryStateIconPipe();
    expect(pipe).toBeTruthy();
  });
});
