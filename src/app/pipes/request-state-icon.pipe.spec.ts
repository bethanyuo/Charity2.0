import { RequestStateIconPipe } from './request-state-icon.pipe';

describe('RequestStateIconPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestStateIconPipe();
    expect(pipe).toBeTruthy();
  });
});
